---
// A slim, centered "relief" band between busier sections — eyebrow + a balanced
// headline (+ optional body), flanked by faint botanical line-art.
import Botanical from './Botanical.astro';
interface Props { eyebrow?: string; title: string; body?: string }
const { eyebrow = 'The promise', title, body } = Astro.props;
---
<section class="relief">
  <Botanical kind="branch" class="bot-l" />
  <Botanical kind="branch" class="bot-r" />
  <div class="wrap relief-in">
    <span class="eyebrow center">{eyebrow}</span>
    <h2 class="serif" set:html={title}></h2>
    {body && <p>{body}</p>}
  </div>
</section>
