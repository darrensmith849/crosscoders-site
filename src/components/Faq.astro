---
import type { Faq } from '../lib/content';
interface Props { items: Faq[] }
const { items } = Astro.props;
---

<div class="faq">
  {items.map((f) => (
    <details>
      <summary>{f.q}</summary>
      <p>{f.a}</p>
    </details>
  ))}
</div>
